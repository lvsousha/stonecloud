<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stone.webapp.mapper.UserMapper">

    <resultMap type="com.stone.webapp.model.User" id="userResult">
        		<result property="id" column="id" />
        		<result property="name" column="name" />
        		<result property="password" column="password" />
        		<result property="email" column="email" />
        		<result property="phone" column="phone" />
        		<result property="created" column="created" />
        		<result property="updated" column="updated" />
    </resultMap>

    <insert id="insertUser" parameterType="com.stone.webapp.model.User">
		insert into users(
						name,
						password,
						email,
						phone,
						created,
						updated
		) values (
					#{name,jdbcType=VARCHAR},
					#{password,jdbcType=VARCHAR},
					#{email,jdbcType=VARCHAR},
					#{phone,jdbcType=VARCHAR},
					#{created,jdbcType=DATE},
					#{updated,jdbcType=DATE}
		)
	</insert>

    <select id="selectAll" resultMap="userResult">
        select * from users
    </select>
    
    <select id="selectUser" parameterType="int" resultMap="userResult">
        select * from users where id=#{id}
    </select>

</mapper>